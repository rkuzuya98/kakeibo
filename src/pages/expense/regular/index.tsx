import type { NextPage } from "next";
import ExpenseRegularPageComponent from "@/components/pages/expense/regular";

const ExpenseRegularPage: NextPage = () => {
  return <ExpenseRegularPageComponent />;
};

export default ExpenseRegularPage;
