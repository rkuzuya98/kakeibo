import type { NextPage } from "next";
import ExpenseSpecialPageComponent from "@/components/pages/expense/special";

const ExpenseSpecialPage: NextPage = () => {
  return <ExpenseSpecialPageComponent />;
};

export default ExpenseSpecialPage;
