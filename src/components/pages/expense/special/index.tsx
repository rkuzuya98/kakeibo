import type { NextPage } from "next";

const ExpenseSpecialPageComponent: NextPage = () => {
  return (
    <div>
      <div>特別出費</div>
    </div>
  );
};

export default ExpenseSpecialPageComponent;
